#include <bits/stdc++.h>
using namespace std;


int solve(int a, int b){
	int ans=min(max(2*a,b),max(2*b,a));
	return ans*ans;
}

int main() {
	int t,n;
	cin>>t;
	while(t--){
	    int a,b;
		cin>>a>>b;
		cout<<solve(a,b)<<"\n";
	}
	return 0;
}
